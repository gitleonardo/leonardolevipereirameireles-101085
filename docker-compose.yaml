services:
  postgres:
    image: postgres:16
    container_name: musicapi-postgres
    environment:
      POSTGRES_DB: musicapi
      POSTGRES_USER: musicapi
      POSTGRES_PASSWORD: musicapi
    ports:
      - "5432:5432"
    volumes:
      - musicapi_pgdata:/var/lib/postgresql/data

volumes:
  musicapi_pgdata:
